import express from "express"; // This line imports the Express.js library into your file.

const router = express.Router() // This line creates a new router object using express.Router().A router is a mini-app within your Express application that allows you to group related routes together.

export default router // This line exports the router object as the default export of the module.